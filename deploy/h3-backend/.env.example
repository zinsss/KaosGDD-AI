KAOS_ROOT=/srv/kaos
GOVERNOR_STATE_ROOT=/srv/kaosgdd/kaosgovernor
KAOS_HOST_GID=1000
GOVERNOR_BIND_ADDRESS=127.0.0.1
BACKEND_BIND_ADDRESS=127.0.0.1
EDGE_BIND_ADDRESS=127.0.0.1

# n8n is an independent, loopback-only backend until an authenticated HTTPS
# route is explicitly approved. Its database password and encryption key live
# in /srv/kaos/secrets/n8n.env and are generated by `kaos-h3 n8n-setup`.
N8N_BIND_ADDRESS=127.0.0.1
N8N_HOST=127.0.0.1
N8N_PROTOCOL=http
N8N_EDITOR_BASE_URL=http://127.0.0.1:5678
N8N_WEBHOOK_URL=http://127.0.0.1:5678/
N8N_SECURE_COOKIE=false
N8N_PROXY_HOPS=0
N8N_TIMEZONE=Asia/Seoul
N8N_EXECUTIONS_MAX_AGE_HOURS=336
N8N_EXECUTIONS_MAX_COUNT=10000

GOVERNOR_BRAIN_TOOLS_HOST=0.0.0.0
GOVERNOR_BRAIN_TOOLS_BIND_ADDRESS=127.0.0.1
GOVERNOR_BRAIN_TOOLS_PORT=8098
GOVERNOR_OPERATION_STORE=postgres
IOS_SHORTCUTS_TOKEN_FILE=/run/secrets/ios_shortcuts_token
IOS_FAX_SHORTCUT_TOKEN_FILE=/run/secrets/ios_fax_shortcut_token
IOS_SCRIBBLE_SHORTCUT_TOKEN_FILE=/run/secrets/ios_scribble_shortcut_token
IOS_PAPERLESS_SHORTCUT_TOKEN_FILE=/run/secrets/ios_paperless_shortcut_token
GOVERNOR_API_INTERNAL_URL=http://governor-api:8096
LOG_LEVEL=INFO

CALENDAR_ADAPTER_INTERNAL_URL=http://calendar-adapter:8091
# Read-only host maintenance report. A system-level systemd timer refreshes it
# daily and after boot. It checks cached package-update
# counts, Docker package update counts, reboot-required, disk/memory, Docker
# health summary, and repo status. It never upgrades, pulls images, restarts, or
# reboots hosts.
SYSTEM_MAINTENANCE_TARGETS=kaosgdd=local:/srv/projects/KaosGDD-AI,kaosbrain=ssh:zin@kaosbrain:/srv/projects/KaosGDD-AI,kaosclinic=ssh:zin@kaosclinic:
SYSTEM_MAINTENANCE_TIMEOUT_SECONDS=12
SYSTEM_MAINTENANCE_REMINDERS_ENABLED=true
SYSTEM_MAINTENANCE_REMINDER_POLL_SECONDS=3600
SYSTEM_MAINTENANCE_REPORT_MAX_AGE_SECONDS=172800
SYSTEM_MAINTENANCE_REPORT_PATH=/data/notifications/maintenance-report.json
SYSTEM_MAINTENANCE_REMINDER_STATE_PATH=/data/notifications/maintenance-reminders.json

# KaosBrain receives only this bearer token. It never receives the Memos PAT.
GOVERNOR_API_TOKEN_FILE=/run/secrets/governor_api_token
MEMOS_SEARCH_ENABLED=false
MEMOS_BASE_URL=http://memos:5230
MEMOS_ACCESS_TOKEN_FILE=/run/secrets/memos_access_token
MEMOS_CREATOR=users/zin
MEMOS_TIMEOUT_SECONDS=15
MEMOS_SEARCH_MAX_RESULTS=20
MEMOS_ATTACHMENT_MAX_MB=20
PAPERLESS_BASE_URL=
PAPERLESS_PUBLIC_URL=
PAPERLESS_API_TOKEN_FILE=/run/secrets/paperless_api_token
PAPERLESS_DEFAULT_OWNER_ID=
PAPERLESS_INBOX_MAX_ATTACHMENT_MB=20
PAPERLESS_TIMEOUT_SECONDS=60
# Optional Paperless tag suggestions. Governor sends OCR/title/tag context to
# KaosBrain and filters the answer back to existing Paperless tags before the
# PWA can preview/apply metadata.
DOCUMENT_TAG_AI_URL=
DOCUMENT_TAG_AI_TIMEOUT_SECONDS=20
AI_TASKS_BRAIN_URL=
# Optional; if blank and AI_TASKS_BRAIN_URL ends in
# /internal/ai-tasks/official-doc-memo/preview, Governor derives
# /internal/ai-tasks/web/preview from it.
AI_TASKS_WEB_BRAIN_URL=
AI_TASKS_BRAIN_TIMEOUT_SECONDS=60
# Optional KaosPACS-AIO second-look forwarding target on KaosBrain.
# AIO calls Governor /tools/imaging/second-look; Governor forwards there.
IMAGING_SECOND_LOOK_URL=
IMAGING_SECOND_LOOK_TOKEN_FILE=/run/secrets/governor_api_token
IMAGING_SECOND_LOOK_TIMEOUT_SECONDS=180

MAIL_NAVER_ENABLED=false
# The Governor worker polls IMAP and queues only `Mail received.`.
MAIL_NAVER_OWNER=worker
MAIL_NAVER_HOST=imap.naver.com
MAIL_NAVER_PORT=993
MAIL_NAVER_USERNAME=
MAIL_NAVER_PASSWORD_FILE=/run/secrets/naver_mail_password
MAIL_NAVER_FOLDERS=세무사,영덕군보건소
# Only these exact/nested folders create notification inbox and Web Push alerts.
MAIL_NAVER_NOTIFICATION_FOLDERS=세무사,영덕군보건소
MAIL_NAVER_STATE_PATH=/data/mail/naver-discord.json
MAIL_NAVER_POLL_SECONDS=60
MAIL_NAVER_TIMEOUT_SECONDS=20
MAIL_NAVER_MAX_ATTACHMENT_MB=20
MAIL_NAVER_PREVIEW_CHARS=2200
MAIL_NAVER_MARK_EXISTING_ON_FIRST_RUN=true

# Fax transport remains on Office Kaos. Leave disabled on the H3 backend until
# the authenticated office Fax Connector is deployed and verified.
FAX_ENABLED=false
# The worker archives/reconciles fax actions and queues only final alerts.
FAX_LIFECYCLE_OWNER=worker
FAX_MESSAGE_INTAKE_ENABLED=false
FAX_STATE_PATH=/data/fax/state.json
FAX_TRANSPORT=connector
FAX_CONNECTOR_BASE_URL=
FAX_CONNECTOR_TOKEN_FILE=/run/secrets/fax_connector_token
FAX_CONNECTOR_TIMEOUT_SECONDS=20
FAX_OUTGOING_QUEUE_ROOT=/integrations/fax-outgoing
FAX_LEGACY_OUTGOING_STATE_PATH=/integrations/fax-outgoing/state.json
FAX_HYLAFAX_RECVQ=/integrations/hylafax/recvq
FAX_HYLAFAX_XFERFAXLOG=/integrations/hylafax/log/xferfaxlog
FAX_HYLAFAX_DONEQ=/integrations/hylafax/doneq
FAX_POLL_SECONDS=20
FAX_MIN_FILE_AGE_SECONDS=60
FAX_MAX_PDF_MB=20
FAX_MARK_EXISTING_ON_FIRST_RUN=true
FAX_DELETE_SOURCE_ON_SUCCESS=true
FAX_RETENTION_DAYS=90
# Daily morning digest alerts are created in the notification inbox/Web Push.
DAILY_DIGEST_ENABLED=false
DAILY_DIGEST_OWNER=worker
DAILY_DIGEST_TIME=07:00
DAILY_DIGEST_PROFILE=main
DAILY_DIGEST_WEATHER_CITY=pohang
DAILY_DIGEST_PORTAL_URL=https://kaosgdd.net
DAILY_DIGEST_STATE_PATH=/data/notifications/daily-digest.json
DAILY_DIGEST_CONTENT_CACHE_PATH=/data/notifications/daily-content.json
DAILY_DIGEST_CONTENT_REFRESH_HOURS=168
DAILY_DIGEST_CONTENT_TIMEOUT_SECONDS=30
DAILY_DIGEST_POLL_SECONDS=30
DAILY_DIGEST_MAX_ITEMS=5
AI_TASK_TEXTBOOK_SEARCH_ENABLED=true
AI_TASK_TEXTBOOK_INDEX_PATH=/data/textbooks/harrison/index/harrison22.index.sqlite
# Direct text alerts through the Pushover iPhone/watchOS app.
# Mirrors daily, fax, mail, maintenance, and system alerts. Tasks stay on native iOS.
PUSHOVER_ENABLED=false
PUSHOVER_APP_TOKEN_FILE=/run/secrets/pushover_app_token
PUSHOVER_USER_KEY_FILE=/run/secrets/pushover_user_key
PUSHOVER_STATE_PATH=/data/notifications/pushover.json
NOTIFICATION_INBOX_ENABLED=true
NOTIFICATION_INBOX_STATE_PATH=/data/notifications/inbox.json
NOTIFICATION_INBOX_RETAINED_ACKNOWLEDGED=2000
# Compatibility fallback for old queued records. Current producers persist
# per-alert priority: routine messages use 0 and actionable failures use 1.
PUSHOVER_PRIORITY=0
PUSHOVER_TIMEOUT_SECONDS=10
PUSHOVER_POLL_SECONDS=5
# The independent Governor worker is the only delivery owner.
PUSHOVER_DELIVERY_MODE=worker
# Personal KaosGDD PWA Web Push. Lock-screen payloads contain generic text only.
WEB_PUSH_ENABLED=false
WEB_PUSH_VAPID_PRIVATE_KEY_FILE=/run/secrets/web_push_vapid_private_key
WEB_PUSH_VAPID_SUBJECT=https://kaosgdd.net
WEB_PUSH_SUBSCRIPTIONS_PATH=/data/notifications/web-push-subscriptions.json
WEB_PUSH_OUTBOX_PATH=/data/notifications/web-push-outbox.json
WEB_PUSH_TIMEOUT_SECONDS=10
GOVERNOR_WORKER_STATE_PATH=/data/notifications/governor-worker.json
GOVERNOR_WORKER_HEALTH_STALE_SECONDS=60
